package Strings;

public class Tester {

	public static void main(String[] args) {
		
		
	
	/*  //implement an algorithm to determine if a string has all unique characters	
		String IsUnique="ShsHaA";
		String NotUnique="shalakha";
		IsUnique obj=new IsUnique();
	    //Case1:using additional Structure for example HashTable
		//a)Enter the unique value
	    System.out.println(obj.usingHashTable(IsUnique));
	    //b)Enter the non unique value
	    System.out.println(obj.usingHashTable(NotUnique));
	    //Case2:assuming characters are only in ASCII 
	   //Case1:using additional Structure for example HashTable
		//a)Enter the unique value
	    System.out.println(obj.check(IsUnique));
	    //b)Enter the non unique value
	    System.out.println(obj.check(NotUnique));*/
    
		
		
/*	//Check if two strings are permutation of each other
		String text1="shala  ";
		String text2="  shaal";
		Permutation obj=new Permutation();
		//Case1: If String contains leading and trailing spaces.
		System.out.println(obj.isPermutation(text1, text2));
		//Case2:If String contains same character 
		String text3="aaaabbb";
		String text4="abababa";
		System.out.println(obj.isPermutation(text3, text4));*/
		
		
/*	 //URLify a given string (Replace spaces is %20) Input : "Mr John Smith   ", 13 Output : Mr%20John%20Smith
		ReplaceAllSpaceWithString obj=new ReplaceAllSpaceWithString();
		String Input="Mr John Smith    ";
		System.out.println(obj.replaceSpaceFunction(Input));
		*/
		
		
/*     //Implement a method to perform basic string compression using the counts of repeated characters. For example, the string aabcccccaaa would become a2b1c5a3. If the "compressed" string would not becomesmaller than the original string, your method should return the original string.
	    String input="aabcccccaaa";
		StringCompression obj=new StringCompression();
		System.out.println(obj.compressionFunction(input));*/
		
		
		
/*		//Assume you have a method isSubstring which checks if one word is a substring of another Given two strings, s1 and s2, write code to check if s2 is a rotation of s1 using only one call to isSubstring (i e , “waterbottle” is a rotation of “erbottlewat”)
		String string1="waterbottle";
		String string2="erbottlewat";
		IsSubstringCheck obj=new IsSubstringCheck();
		System.out.println(obj.IsSubString(string1,string2));*/
		
		
	}

}
